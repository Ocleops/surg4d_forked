pred_root: ${eval.temporal.output_dir}
labels_root: ${eval.temporal.labels_root}
labels_filename_template: ${eval.temporal.labels_filename_template}
output_dir: ${compute_metrics.output_dir}/temporal
aggregated_output_filename: ${compute_metrics.temporal.output_dir}/aggregated.json

